import { createContext } from "react";

export const PeixariaContext = createContext({});

export const PeixariaProvider = ({children}:any)-> {
    async function registerPeixaria(peixaria: Peixaria, urlDevice: string) Promise<string>{
        
    }
}